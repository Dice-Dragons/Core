name: Publish Release

on:
  release:
    types: [published]

jobs:
  xenforo-publish:
    uses: ./.github/workflows/xenforo-release-publisher.yml@main
    with:
      dry_run: true 
    secrets: inherit
