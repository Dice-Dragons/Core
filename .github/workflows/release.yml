name: Publish Release

on:
  release:
    types: [published]

jobs:
  xenforo-publish:
    uses: ./.github/workflows/xenforo-release-publisher.yml
    secrets:
      XENFORO_API_KEY: ${{ secrets.XENFORO_API_KEY }}
      XENFORO_API_USER_ID: ${{ secrets.XENFORO_API_USER_ID }}
