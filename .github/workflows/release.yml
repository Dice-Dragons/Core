name: Publish Release

on:
  release:
    types: [published]

jobs:
  xenforo-publish:
    uses: Dice-Dragons/Core/.github/workflows/xenforo-release-publisher.yml@main
    with:
      dry_run: true   # <-- Hier steuerst du fürs Testen, ob es ein Dry Run ist
    secrets: inherit
